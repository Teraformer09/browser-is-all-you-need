| File / component | Exact change | Why / audit items | Verification and boundary |
|---|---|---|---|
| Source layout (v0.7.0) | Move equivalent roles into `harness/`, `agents/`, `verification/` and `integrations/`; group all 70 existing predicates by policy role; make the backend private to the task. | Avoid a global `runtime` package and keep source responsibilities explicit. | 76 offline tests pass; no model evaluation launched. |
| Canonical specification and packaging (v0.7.0) | Package `specs/task.json` as the sole task-parameter source; derive verifier expectations from it; fingerprint every implementation; remove obsolete wheel force-includes. | Prevent task/registry drift and missing installed resources. | Wheel and sdist build, isolated wheel installation and APK compilation checked. Android build-tools 34 fallback is used when 35 is absent; tool availability is checked before generated output is rebuilt. |
| `app/dummy_android_app/src/.../MainActivity.java` and root app copy | Centralized guarded selection handlers; hide controls until native prerequisites; validate before state mutation; show rejection/stage; invalidate downstream selections on edits; synchronous text persistence; truthful selection highlighting; append actual state mutations. | Fixes 1–4, 8; enables replay verifier. | Earlier app guard build passed 24 Android assertions before the new journal/probe additions. Latest source builds; no new live episode after stop. |
| Both `AndroidManifest.xml` files + `VerifierStateProvider.java` | Add DUMP-protected, read-only live-state provider; reject insert/update/delete; bounded UI-thread snapshot capture. | Genuine fifth state-evidence strategy, without actor actions or write probing. | Compiles; live provider permission/capture validation pending. |
| `uber_clone_031/harness.py` | Strict object parsing; separate schema, permission, execution, app acceptance and rejection reason; early finish continues; fresh unique UI dumps; no automatic scrolling; trace every internal ADB call; safe quoted text; source APK identity; mutation/probe readers. | Fixes 5, 6, 9, 10. | Offline malformed-action, recovery, stale-cache, trace and quoting tests pass. |
| `uber_clone_031/verifier.py` | Six independent stage predicates, current/ever-completed status, invalidation and rejection histories. | Fix 7; retains completed work visibility. | Stage coverage tests; **diagnostics only**, not primary reward. |
| `verifier_specs/` | Four schemas plus app, topic, task, validity, interaction, scoring and policy registries. Fixed 14 IDs and five distinct bindings each. | Separates policies, app knowledge, requested values and evidence. | Schema/registry and task-YAML consistency checks; 70 entries. |
| `policy_registry.py`, `policy_evidence.py`, `policy_verifiers.py`, `policy_runner.py` | Allowlisted implementations; 70 dedicated entry points; fresh evidence readers, full mutation replay, per-result reasons/refs and immutable implementation/spec fingerprints. | Implements 14×5 dedicated verifiers; never counts a missing channel as PASS. | Offline happy/missing coverage for every entry, plus wrong payment, semantic mismatch, stale data and journal-gap cases. |
| `policy_scoring.py` | Exact integer-tenths threshold; PASS=1, INVALID=0, FAIL=-1; FAIL-first episode aggregation; explicit INVALID training filter. | Replaces binary/fractional primary score; preserves agreed two-PASS threshold. | All 243 five-vote combinations and signed-adapter tests. External trainer not modified. |
| `evidence.py`, `ocr.py` | Store per-action PNG/XML, ADB phases, live snapshots, mutation journal and bounded OCR receipts; terminal 70-verifier verdict and policy files; preflight required evidence/APK. | Evidence-backed voting and diagnostics, not claimed tool success. | Offline evidence/uncertainty tests; live new capture path pending approval. |
| `run_evidence.py`, `prime_env.py` | Same policy engine in local/Prime adapters; verdict-based success, negative reward preserved, missing verdict explicitly INVALID; guards require explicit evaluation approval. | Prevents reward clipping/default ambiguity and unapproved second eval. | CLI, shell, Prime loader guards tested without device execution. |
| Build/install scripts and `Dockerfile`, `.dockerignore` | Source-aware APK cache, installed hash comparison, no silent uninstall; OCR/runtime packages; safe readiness-only default; exclude credentials and artifacts from image context. | Stale APK/readiness fixes. | APK/image built; readiness-only tool/KVM inspection. |
| `pyproject.toml` | Package v0.5.0 bundles existing runtime and verifier JSON; replace private Hub wheel (HTTP 404) with tested public Python dependencies. | Fixes reproducible installation failure. | Clean local installation and standalone loader tests; remote Prime CI not claimed. |
| `tests/`, `readiness.py`, docs | Add scoring, 70-entry evidence, input, interaction, approval and training-filter tests; document flow, commands and boundaries. | Fix 11 and reproducibility. | Tests and preparation only after stop; no second eval. |
| Existing `uber_clone_031/integrations/prime_upload.py` | No expanded-payload change applied. | Approval review blocked transmitting additional screenshots/state/ADB/probe payload. | No new upload, publication or dashboard-rendering claim. |
| `progress.py`, `evidence.py` (v0.6.0) | Evaluate the unchanged 70 predicates on each evidence prefix; record checkpoint policy rewards, pending goals, newly completed/invalidated stages, JSON checkpoints and a Markdown timeline. Terminal reward remains separate. | Completed pickup now shows T1=+1 immediately; intermediate zero is not mislabelled INVALID; repeated actions cannot accumulate reward. | Five new offline reporting tests; 62 total source/image tests. |
| Scoped task `MainActivity.java` (v0.6.0) | Disable Now until ride type and destination are set; disable location shortcuts before ride type; enforce initial UI enablement after attachment; disable Book with empty pickup; correct premature status text. | UI controls and harness prerequisites agree. No expected value, action budget or scoring rule changed. | 29 actual Android contract assertions pass. The legacy root app copy was not altered in this revision. |
| `run_evidence.py`, `prime_env.py`, `upload_evidence.py` (v0.6.0) | Emit PENDING until terminal scoring, retain policy checkpoint rewards, export per-policy/per-stage Prime metrics and complete checkpoint files; clarify type_text's built-in focus and the two different ride selections in the prompt. | Make positive work visible without summing it into the final episode reward or repairing model actions. | Offline export/adapter tests plus live validation receipts; model success is reported separately from oracle validation. |
| `scripts/build_apk.sh` (v0.6.0) | Canonicalize the build-script path used in the source fingerprint. | Relative versus absolute invocation no longer forces an unnecessary APK rebuild. | Readiness APK hash equals the installed smoke-test APK hash. |
| `action_format.py`, `run_evidence.py`, `scripts/run_eval.sh`, `policy_registry.py`, `pyproject.toml` (v0.6.1) | Add opt-in strict API JSON schema, require a capable endpoint, fingerprint the static schema adapter, record response mode/schema hash, preserve default text mode. No returned action repair, response healing or verifier/scoring change. | Dots completed all six stages but its prose-prefixed first reply failed G1/G2/G4/T7; preserve that run and validate a distinct structured-output interface. | Eight new syntax/request tests (70 standalone total), matching built source, zero-cost API schema preflight; Android APK hash unchanged. |
| `action_format.py`, `run_evidence.py`, `pyproject.toml`, `tests/test_action_format.py` (v0.6.2) | Add explicit JSON-object response mode; keep text mode as default and schema mode selectable. Record the chosen interface in metadata; do not repair returned actions. | Genuine Dots JSON-only run passes all 14 policies in 9 actions using the unchanged APK, task, 12-action limit and score contract. | Frozen replay reproduces +1; Prime verifies all 10 screenshots and 90 original logs. |
| `uber_clone_031/integrations/prime_upload.py`, `tests/test_upload_and_retry.py` | Add bounded payload-size override and references to original conversation PNGs instead of embedding them twice; retain inline capture for error-only frames and verify references, hashes and logs. | Larger archived trace exceeded the local 25 MiB cap and received HTTP 413 with duplicated image data. Deduplication preserves all evidence and reduces its payload to 26,367,353 bytes. | Latest standalone suite: 73 tests pass. No pixels or original evidence were edited. |
| Qodo follow-up: `verification/evidence_readers.py`, `tests/test_policy_verifiers.py` | Exclude only the app's `Booking step N/5 • Pickup: set/not set` diagnostic from route-label counting, retaining the stage. Fixtures now include that real progress text. Other repeated labels and duplicate route summaries are still rejected. OCR collection is unchanged; both visual readers use the corrected parser. | Fix the reproduced T1–T6 accessibility/OCR false INVALID results without weakening duplicate detection or changing scoring. | All 12 visual slots pass the corrected fixture; duplicated route nodes/text remain INVALID. Saved terminal UI parses correctly. OCR tests mock recognition, not pixels. |
| Qodo follow-up: `harness/backend/apk_env.py`, `harness/backend/core/actions.py` | Route raw strings/decoded objects through the same schema parser; reject non-object payloads and non-string action types before mapping operations. | Fix native AttributeError/TypeError escaping the base backend. The already-guarded active RideStageEnv remains compatible. | Regression cases preserve the configured penalty, action receipt, one-step accounting and recovery after rejection. |
| Qodo follow-up: `harness/backend/policies/openai_policy.py` | Remove the missing backend.config import; explicit API key takes precedence over OPENAI_API_KEY, with the existing missing-key error deferred until construction. | Make the optional bundled policy importable without changing the active OpenRouter agent. | Source import/key tests and a separately installed wheel import pass; no OpenAI request made. |
| Qodo follow-up: `tests/test_review_regressions.py` | Add 17 offline regression tests for app-accurate progress, OCR region handling, duplicate evidence, malformed base-backend actions and optional-policy imports. | Cover the cases missed by the original 76-test suite. | 93 offline tests pass; wheel/sdist build and installed 70-entry registry check pass. Temporary package-test outputs stay under /data/Tirtha and are removed. No live eval, Git commit or PR update in this patch step. |
