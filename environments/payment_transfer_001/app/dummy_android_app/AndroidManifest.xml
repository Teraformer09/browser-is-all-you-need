<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- No INTERNET, banking, contacts, SMS or location permissions. -->
    <application android:theme="@style/Theme.PaymentDemo" android:label="Payment Demo"
        android:allowBackup="false" android:supportsRtl="true">
        <activity android:name=".MainActivity" android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <provider android:name=".VerifierStateProvider"
            android:authorities="com.primeintellect.paymentdemo.verifier"
            android:exported="true" android:permission="android.permission.DUMP"/>
    </application>
</manifest>
